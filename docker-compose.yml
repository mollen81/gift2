services:
  backend:
    build:
      context: ./backend
    container_name: invitation-backend
    restart: always
    env_file:
      - .env
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    container_name: invitation-frontend
    restart: always
    ports:
      - "80:80"
      - "8443:8443"
    volumes:
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge